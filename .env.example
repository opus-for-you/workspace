# Database Configuration
# PostgreSQL connection string - automatically provided by Replit
DATABASE_URL=postgresql://username:password@host:port/database

# Postgres Connection Details (auto-populated by Replit)
PGHOST=your-postgres-host
PGPORT=5432
PGUSER=your-postgres-user
PGPASSWORD=your-postgres-password
PGDATABASE=your-database-name

# Session Secret (generate a random string for production)
# Automatically generated by Replit
SESSION_SECRET=your-secret-key-here

# AI/LLM Integration (Optional)
# For AI-powered goal suggestions and reflection prompts
# If not provided, the app will use fallback suggestions

# Anthropic API Key (Preferred) - Used for Claude-powered suggestions
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# OpenAI API Key (Fallback) - Used for GPT-4o-mini suggestions
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Push Notifications (Optional - Future Feature)
# VAPID keys for web push notifications
# Generate with: npx web-push generate-vapid-keys
# VAPID_PUBLIC_KEY=your-public-key
# VAPID_PRIVATE_KEY=your-private-key
# VAPID_SUBJECT=mailto:your-email@example.com

# Application Configuration
NODE_ENV=development
PORT=5000

# GitHub Integration (Optional)
# Used for automated documentation syncing to GitHub
# GITHUB_TOKEN=your-github-personal-access-token
